{
  "description": "a api function",
  "memory": 128,
  "timeout": 10,
  "handler": "dist.default",
  "hooks": {
    "build": "../../node_modules/.bin/webpack --config ../../webpack.config.js --bail",
    "clean": "../../node_modules/.bin/rimraf dist"
  }
}
